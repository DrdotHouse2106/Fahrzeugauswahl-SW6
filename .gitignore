/vendor/
/node_modules/
*.zip
.DS_Store
.idea/
.php-cs-fixer.cache

# Das kompilierte Storefront-JS (dist/) wird BEWUSST mitversioniert,
# damit das Plugin auf Produktivsystemen ohne Node/npm läuft
# (theme:compile reicht dann, kein bin/build-storefront.sh nötig).
